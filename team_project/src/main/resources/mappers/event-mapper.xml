<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.team.mappers.evevt">

	<insert id="insert">
		insert into tbl_event
			(event_no, event_title, event_content, event_image, event_end_date)
		values
			(seq_event_eventno.nextval, #{event_title}, #{event_content}, #{event_image, jdbcType=VARCHAR}, #{event_end_date, jdbcType=DATE})
	</insert>

</mapper>