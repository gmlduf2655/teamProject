<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.team.mappers.user">
	
	<!-- 회원 추가 -->
	<insert id="insertUser">
		insert into tbl_user
			(userno, userid, userpw, nickname, username, email, address, cellphone, profile_image)
		values
			(seq_user_userno.nextval, 
			#{userid}, 
			#{userpw}, 
			#{nickname}, 
			#{username}, 
			#{email, jdbcType=VARCHAR}, 
			#{address, jdbcType=VARCHAR}, 
			#{cellphone, jdbcType=VARCHAR}, 
			#{profile_image, jdbcType=VARCHAR})
	</insert>
	
	<!-- 간편로그인 회원 추가 -->
	<insert id="insertSnsUser">
		insert into tbl_user
			(userno, nickname, username, cellphone, profile_image, sns_id, sns_type, sns_connect_date)
		values
			(seq_user_userno.nextval, 
			#{nickname, jdbcType=VARCHAR}, 
			#{username}, 
			#{cellphone, jdbcType=VARCHAR}, 
			#{profile_image, jdbcType=VARCHAR},
			#{sns_id},
			#{sns_type},
			sysdate)
	</insert>
	
	<!-- 아이디로 회원 조회 -->
	<select id="selectUserById" resultType="UserVo">
		select * from tbl_user
		where userid= #{userid}
	</select>
	
	<!-- 유저번호로 회원 조회 -->
	<select id="selectUserByUserno" resultType="UserVo">
		select * from tbl_user
		where userno = #{userno}
	</select>
	
	<!-- 아이디, 비밀번호로 회원 조회 -->
	<select id="selectUserByIdAndPwd" resultType="UserVo">
		select * from tbl_user
		where userid= #{userid} and userpw = #{userpw}
	</select>
	
	<!-- 닉네임으로 회원 조회 -->
	<select id="selectUserByNickname" resultType="UserVo">
		select * from tbl_user
		where nickname= #{nickname}
	</select>	
		
	<!-- 이메일로 회원 조회 -->
	<select id="selectUserByEmail" resultType="UserVo">
		select * from tbl_user
		where email= #{email}
	</select>
	
	<!-- 간편로그인 아이디와 종류로 회원 조회 -->
	<select id="selectUserBySnsIdAndType" resultType="UserVo">
		select * from tbl_user
		where sns_id = #{sns_id} and sns_type = #{sns_type}
	</select>
		
	<!-- 유저 목록 조회 -->
	<select id="userList" resultType="UserVo">
		select * from tbl_user
		order by reg_date desc 
	</select>
	
	<!-- 기존유저 목록 조회 -->
	<select id="originUserList" resultType="UserVo">
		select * from tbl_user
		where sns_type is null
		order by reg_date desc 
	</select>
	
	<!-- 간편로그인유저 목록 조회 -->
	<select id="snsUserList" resultType="UserVo">
		select * from tbl_user
		where sns_type is not null
		order by reg_date desc 
	</select>
	
	<!-- 유저 정보 수정 -->
	<update id="updateUser">
		update tbl_user set
			nickname = #{nickname},
			email = #{email},
			address = #{address, jdbcType=VARCHAR},
			cellphone = #{cellphone, jdbcType=VARCHAR},
			profile_image = #{profile_image, jdbcType=VARCHAR},
			mod_date = sysdate
		where userid = #{userid}
	</update>
	
	<!-- 유저 비밀번호 수정 -->
	<update id="updateUserpw">
		update tbl_user set
			userpw = #{userpw},
			mod_date = sysdate
		where userid = #{userid}
	</update>
	
	<!-- 유저 포인트 수정 -->
	<update id="updateUserPoint">
		update tbl_user set
			point = point + #{point}
		where userno = #{userno}
	</update>
		
	<!-- 유저 삭제 -->
	<update id="deleteUser">
		update tbl_user 
			set user_status='0'
		where userno = #{userno}
	</update>
</mapper>