<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.team.mappers.cinema">

	<insert id="insertCinema">
		insert into tbl_cinema(
			cinema_no,
			cinema_name,
			cinema_address,
			cinema_seat_count,
			cinema_status,
			cinema_begin_date,
			cinema_finish_date
			)
		values(
			seq_cinema_no.nextval,
			#{cinema_name},
			#{cinema_address},
			#{cinema_seat_count},
			#{cinema_status, jdbcType=VARCHAR},
			#{cinema_begin_date, jdbcType=VARCHAR},
			#{cinema_finish_date, jdbcType=VARCHAR}
			)
	</insert>
	
	
	
	
	
	<update id="updateCinemaInfo">
		update tbl_cinema set
			cinema_name			= #{cinema_name}, 
			cinema_address		= #{cinema_address},
			cinema_seat_count	= #{cinema_seat_count}, 
			cinema_status		= #{cinema_status, jdbcType=VARCHAR}, 
			cinema_begin_date	= #{cinema_begin_date, jdbcType=VARCHAR}, 
			cinema_finish_date	= #{cinema_finish_date, jdbcType=VARCHAR}
		where cinema_no = #{cinema_no} 
	</update>
	
	
	
	
	
	<select id="getCinemaList" resultType="CinemaVo">
		select * from tbl_cinema
		order by ${column} ${orderType}
	</select>
	
	
	
	
	
	<select id="getCinemaInfo" resultType="CinemaVo">
		select * from tbl_cinema
		where cinema_no = #{cinema_no}
	</select>
	
	
	
	
	
	<update id="updateCinemaStatus">
		update tbl_cinema set
			cinema_status = #{cinema_status}
		where cinema_no = #{cinema_no}
	</update>

</mapper>